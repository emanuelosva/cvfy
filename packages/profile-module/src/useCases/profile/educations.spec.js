const {
  makeAddProfileEducation,
  makeUpdateProfileEducation,
  makeRemoveProfileEducation,
} = require('./educations')

describe('JobsUseCases', () => {
  describe('AddEducations', () => {
    test.todo('it should return the added educations')
  })
  describe('UpdateEducations', () => {
    test.todo('it should return the updated educations')
  })
  describe('RemoveEducations', () => {
    test.todo('it should call the remove factory method with project model')
  })
})
