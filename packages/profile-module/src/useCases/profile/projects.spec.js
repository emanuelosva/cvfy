const {
  makeAddProfileProject,
  makeRemoveProfileProject,
  makeUpdateProfileProject,
} = require('./projects')

describe('ProjectsUseCases', () => {
  describe('AddProject', () => {
    test.todo('it should return the added project')
  })
  describe('UpdateProject', () => {
    test.todo('it should return the updated project')
  })
  describe('RemoveProject', () => {
    test.todo('it should call the remove factory method with project model')
  })
})
