const {
  makeAddProfileJob,
  makeUpdateProfileJob,
  makeRemoveProfileJob,
} = require('./jobs')

describe('JobsUseCases', () => {
  describe('AddJobs', () => {
    test.todo('it should return the added jobs')
  })
  describe('UpdateJobs', () => {
    test.todo('it should return the updated jobs')
  })
  describe('RemoveJobs', () => {
    test.todo('it should call the remove factory method with project model')
  })
})
